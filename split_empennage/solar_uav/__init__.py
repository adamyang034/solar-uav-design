"""Split-empennage solar UAV toolchain (fork).

Same phases as the π-tail optimizer, but two identical fuselage T-tails,
no H-stab solar, no bridging stabilizer. Constants live in `solar_uav.config`.
"""

__version__ = "0.3.0-split-empennage"
